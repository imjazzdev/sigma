import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  // Primary Colors (top row)
  static const Color mustardYellow = Color(0xFFDCC35C);
  static const Color pink = Color(0xFFF867A5);
  static const Color blue = Color(0xFF003F94);

  // Secondary Colors (middle row)
  static const Color cream = Color(0xFFFDF3D9);
  static const Color softPink = Color(0xFFFBDCD9);
  static const Color white = Color(0xFFFFFFFF);

  // Light Background (bottom left)
  static const Color paleGreen = Color(0xFFF4FAEC);

  // Logo Colors (from logo at the bottom)
  static const Color logoYellow = Color(0xFFE8D060); // similar to mustardYellow
  static const Color logoBlue = Color(0xFF004AAD); // darker blue for logo

  // Background Color (behind the palette)
  static const Color background = Color(
    0xFF1A1A1A,
  ); // approximate dark background
}
